# Node.js
node_modules/

# File di log
*.log

# File temporanei dell'editor
*.swp
.DS_Store

# Cartella di build (se la usi per il backend, ma di solito non serve)
# /build/  <-- Commentata! Rimuovi il commento SOLO se usi una cartella build per il backend

# File specifici di Vercel (se non li usi, puoi rimuovere queste righe)
.vercel

# File specifici di IDEs (IntelliJ, VS Code - opzionali, ma consigliati)
*.iml
*.ipr
*.iws
.idea/
#.vscode/  <-- Generalmente si tiene, ma se vuoi puoi decommentare

# Altri file temporanei (opzionali, ma consigliati)
*.pyc

# File specifici del tuo progetto (se ne hai)
# Aggiungi qui eventuali file o cartelle specifici del *tuo* progetto
# che vuoi ignorare, ma che non rientrano nelle categorie precedenti.

# macOS Finder
.DS_Store